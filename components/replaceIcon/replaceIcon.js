import styles from './replaceIcon.module.scss';

const ReplaceIcon = () => {
    return (
        <div className={styles.replaceIcon}>
            <img src='/replace.svg' width={20} height={20} />
        </div>
    );
}

export default ReplaceIcon;
